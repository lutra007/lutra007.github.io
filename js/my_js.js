$(function () {
    $('#let_kira').each(function (i) {
        var name = "lutra007";
        var host = "gmail.com";
        document.getElementById('let_kira').innerHTML = name + "@" + host;
    });
});
